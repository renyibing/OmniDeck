{
  "name": "omnideck-control-center",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"npm:dev:daemon\" \"npm:dev:web\"",
    "dev:web": "vite",
    "dev:daemon": "tsx watch src/server/index.ts",
    "desktop:dev": "tauri dev",
    "desktop:dev:android": "tsx scripts/desktop-android-dev.ts",
    "desktop:build": "tauri build",
    "desktop:check": "cargo check --manifest-path src-tauri/Cargo.toml",
    "start:daemon": "tsx src/server/index.ts",
    "smoke:android-actions": "tsx scripts/android-action-smoke.ts",
    "build": "tsc -b && vite build",
    "test": "vitest run",
    "lint": "tsc --noEmit && tsc -p tsconfig.server.json --noEmit"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "ws": "^8.21.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@types/node": "^26.2.0",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@types/ws": "^8.18.1",
    "concurrently": "^10.0.4",
    "tsx": "^4.23.12",
    "typescript": "latest",
    "vite": "latest",
    "vitest": "latest"
  }
}
